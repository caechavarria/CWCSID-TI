---
- name: Add a user
  hosts: all
  gather_facts: false
  tasks:
    - name: Add User
      win_user:
        name: "{{ USER_NAME }}"
        password: "{{ PASSWORD }}"
        state: present
...
